[unit]
name = "desktop"
description = "everything I need to work on Windows"
requires = ["komorebi"]

[service]
kind = "Oneshot"
autostart = true
executable = "msg.exe"
arguments = [
    "*",
    "Desktop recipe completed!",
]
